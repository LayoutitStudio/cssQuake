{
  "name": "cssquake",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "type": "module",
  "description": "A standalone CSS-rendered Quake renderer powered by PolyCSS.",
  "license": "GPL-2.0",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "prebuild": "pnpm exec playwright install chromium",
    "prepare:quake": "node src/quake/prepare/prepare-quake.mjs",
    "build": "pnpm typecheck && pnpm prepare:quake && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@layoutit/polycss": "^0.2.2"
  },
  "devDependencies": {
    "7z-bin": "^0.0.8",
    "@types/node": "^25.5.0",
    "esbuild": "^0.28.0",
    "playwright": "^1.58.2",
    "sharp": "^0.34.5",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
